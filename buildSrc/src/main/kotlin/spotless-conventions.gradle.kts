/**
 * Spotless Code Formatting Conventions
 *
 * This Gradle Kotlin DSL convention script configures Spotless for consistent
 * code formatting across the project:
 * - Kotlin source files with KtLint formatting
 * - Kotlin Gradle scripts with tab indentation
 * - Markdown files with space indentation
 * - License header application
 * - Whitespace and newline normalization
 *
 * Usage: Apply to projects requiring code formatting:
 *
